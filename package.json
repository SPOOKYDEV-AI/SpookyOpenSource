{
  "name": "spooky-open-source",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "validate": "npm run lint && npm test && npm run safety:check && npm run pack:check",
    "safety:check": "node scripts/check-public-safety.js",
    "pack:check": "npm pack --workspace packages/jwt-resilience --dry-run && npm pack --workspace packages/safe-service-supervisor --dry-run"
  },
  "engines": {
    "node": ">=20"
  }
}
